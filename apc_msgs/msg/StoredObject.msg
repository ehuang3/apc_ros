# A message for holding object meta-data.

# Path to the object mesh.
string path

# Object name.
string name

# Poses of the object (if an object is comprised of several shapes).
geometry_msgs/Pose[] poses
